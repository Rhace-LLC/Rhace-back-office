/**
 * @deprecated Import from "./useServiceAggregator" instead.
 * Kept for backwards compatibility. The previous write-capable
 * (POST) probes were removed so the debug surface stays read-only.
 */
export { useServiceAggregator } from "./useServiceAggregator";
