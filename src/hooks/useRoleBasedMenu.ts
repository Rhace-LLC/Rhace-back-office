/**
 * Backwards-compatible re-export.
 * The canonical navigation model now lives in `components/layout/menu.ts`.
 */
export { useRoleBasedMenu } from "@/components/layout/menu";
export type { MenuItem } from "@/components/layout/menu";
